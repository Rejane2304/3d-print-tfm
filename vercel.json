{
  "buildCommand": "DIRECT_URL=$DATABASE_URL prisma migrate deploy && prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/expired-orders",
      "schedule": "0 0 * * *"
    }
  ]
}
